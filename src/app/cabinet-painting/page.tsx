import { type Metadata } from "next";

export const metadata: Metadata = {
  alternates: {
    canonical: "/cabinet-painting-mejor",
  },
};

export { default } from "../cabinet-painting-mejor/page";
