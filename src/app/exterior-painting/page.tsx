import { type Metadata } from "next";

export const metadata: Metadata = {
  alternates: {
    canonical: "/exterior-painting-upgrade",
  },
};

export { default } from "../exterior-painting-upgrade/page";
